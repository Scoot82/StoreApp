<h1>Sign up for an account</h1>

<%= form_for @user do |x| %>
	
	<%= x.label :email %>
	<%= x.text_field :email %>
	<%= x.label :password %>
	<%= x.password_field :password %>
	<%= x.submit %>

<% end %>